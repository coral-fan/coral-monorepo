import { getIconStoryConfigurations } from '../utils';
import { LogoIcon } from './LogoIcon';

const { meta, Default } = getIconStoryConfigurations(LogoIcon);

export default meta;

export { Default };
